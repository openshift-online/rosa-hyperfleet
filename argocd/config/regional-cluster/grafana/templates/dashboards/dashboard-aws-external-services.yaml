apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-dashboard-aws-external-services
  namespace: {{ .Release.Namespace }}
  labels:
    grafana_dashboard: "1"
  annotations:
    k8s-sidecar-target-directory: "Infrastructure"
data:
  aws-external-services.json: {{ .Files.Get "dashboards/infrastructure/aws-external-services.json" | quote }}
