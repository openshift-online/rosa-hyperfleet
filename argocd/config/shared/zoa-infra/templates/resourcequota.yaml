apiVersion: v1
kind: ResourceQuota
metadata:
  name: zoa-jobs-quota
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "zoa-infra.labels" . | nindent 4 }}
spec:
  hard:
    pods: "50"
    requests.cpu: "10"
    requests.memory: "20Gi"
    limits.cpu: "20"
    limits.memory: "40Gi"
