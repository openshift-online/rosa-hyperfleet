apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: zoa-jobs-default
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "zoa-infra.labels" . | nindent 4 }}
spec:
  podSelector: {}
  policyTypes:
    - Ingress
  ingress: []
